{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "m010 signal item",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "subject_type",
    "subject_id",
    "category",
    "evidence",
    "timestamp"
  ],
  "properties": {
    "signal_id": {
      "type": "string",
      "pattern": "^signal-[A-Za-z0-9._:-]+$",
      "description": "Stable signal identifier for lifecycle and challenge linkage"
    },
    "subject_type": {
      "type": "string",
      "enum": [
        "CreditClass",
        "Project",
        "Verifier",
        "Methodology",
        "Address"
      ]
    },
    "subject_id": {
      "type": "string",
      "minLength": 1
    },
    "category": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]{1,63}$",
      "description": "Machine-safe category key (snake_case)"
    },
    "endorsement_level": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5
    },
    "signaler_id": {
      "type": "string",
      "minLength": 1
    },
    "status": {
      "type": "string",
      "enum": [
        "submitted",
        "active",
        "challenged",
        "escalated",
        "resolved_valid",
        "resolved_invalid",
        "withdrawn",
        "invalidated"
      ],
      "default": "active",
      "description": "Signal lifecycle state. See SPEC.md section 6.1 for state machine."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "koi_links",
        "ledger_refs"
      ],
      "properties": {
        "koi_links": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "ledger_refs": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "web_links": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    }
  }
}
